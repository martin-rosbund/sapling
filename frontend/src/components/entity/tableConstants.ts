
/**
 * Table and pagination constants for entity and note tables.
 * Provides default values for page size, table height, and options.
 */

/** Default number of rows per page */
export const DEFAULT_PAGE_SIZE = 25;

/** Options for selectable page sizes */
export const PAGE_SIZE_OPTIONS = [10, 25, 50, 100];

/** Default table height in pixels */
export const DEFAULT_TABLE_HEIGHT = 600;

/** Minimum table height in pixels */
export const MIN_TABLE_HEIGHT = 300;

/** Offset used to calculate available table height */
export const TABLE_HEIGHT_OFFSET = 280;