{  
  "name": "sustain",
  "version": "1.0.0",
  "scripts": {
    "start:backend": "npm run start:debug --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently --names \"BACKEND,FRONTEND\" \"npm:start:backend\" \"npm:start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
